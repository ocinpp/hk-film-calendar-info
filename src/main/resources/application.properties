# Output directory of the CSV file
app.outputDirectory=D:\\result

# year
app.year=2018

# Comma seprated list of ids
app.movieIds=243,322,319,255

#Disable LiveReload server 
spring.devtools.livereload.enabled=false

#Disable JMX
spring.jmx.enabled=false

#Disable Endpoints
endpoints.enabled=false

#Disable JMX endpoints
endpoints.jmx.enabled=false